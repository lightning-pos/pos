export async function runMigrations() {
  console.log('Running migrations');
  // Get the current version of the schema

  // Get the latest version of the schema

  // return, if the current version is the latest version

  // if the current version is not the latest version

  // fetch the migration scripts from the server

  // Run the migration scripts sequentially

  // Update the current version of the schema
}
